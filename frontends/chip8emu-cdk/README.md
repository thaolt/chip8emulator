# chip8emu CDK frontend

## Install dependencies

To compile this frontend you need CDK (Curses Development Kit) development package

Example for Ubuntu/Debian:

```
sudo apt install libcdk5-dev
```


